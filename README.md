# HaloPay Mesh Relayer

A Rust-based cryptographic gossip protocol. This relayer allows offline HaloPay Point-of-Sale terminals to sync encrypted transaction batches locally over Bluetooth LE or LoRaWAN. When a node gains internet access, it acts as a relayer to anchor the entire mesh's transactions to the Stellar network.
